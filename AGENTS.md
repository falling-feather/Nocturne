# 夜航项目协作规则

## 唯一日常应用与资料

- 日常桌面应用固定为 `dist/Nocturne-desktop/Nocturne.exe`；桌面“夜航 Nocturne”快捷方式始终指向它，使用正常参数。
- 组织名与应用名保留 `FeatherNote`；从 V0.2.0 起日常资料固定为 `%USERPROFILE%/NocturneData/`，避免 AppData 被打包宿主虚拟化。首次升级先备份旧库，已有新库不被自动覆盖；不得把测试库当日常库。
- 不再提供用户可双击的独立预览启动脚本，也不再创建并列的日常版本目录。
- 历史程序只放在 `dist/_archive/`，旧应用 EXE/CMD 改为非运行扩展名保存；历史资料保留备份。

## 开发、测试与更新

- 测试保持隔离。只有显式设置 `NOCTURNE_ALLOW_TEST_PROFILE=1` 的开发进程才能使用测试/基准参数；不得将该变量持久写入用户环境。
- 自动测试使用自己的 UUID 资料目录；测试样例不能合并进用户文章。
- 便携构建产物集中在 `dist/artifacts/`，不是日常应用入口；发布附件与日常运行目录分开。
- 按用户授权，每次更新验证通过后，正常保存退出旧应用，备份资料与旧程序，原位更新唯一桌面应用并启动供验收。禁止强杀未保存的用户进程。
- 更新前确认实际快捷方式目标与进程路径；不能仅按进程名或旧对话中的 PID 操作。
- 发布版本由用户指定；不能覆盖已发布 GitHub 标签或把工作区构建伪装成新的公开版本。

## 资料维护

- 合并前对源库和目标库分别生成一致性 SQLite 备份，并保存附件。
- 合并必须保留正文、格式、目录与来源映射；同名不同内容保留副本，不覆盖目标现有文章。
- 重复合并应幂等；不能把历史备份或已删除记录自动恢复成新文章。
- 合并明细和实际用户内容放在私有资料备份中，不提交到公开 Git 仓库。
- 按 `doc/00—03` 文档体系登记任务、当前实现与真实提交记录。

- 核验资料必须同时检查真实物理文件路径、正文和附件；同一 AppData 路径字符串及相同记录数不足以证明桌面与开发环境共用资料。
